"""Scenario-scoped router package.

Houses scenario CRUD plus all the per-scenario endpoints that previously
lived under ``/wallets/{wallet_id}/...`` (results, roadmap, currencies,
portals, category priorities, credits) and the new endpoints unique to
the scenario model (future-card CRUD, overlay CRUD).
"""
