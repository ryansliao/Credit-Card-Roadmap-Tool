# RoadmapTool Phase 7 Migration Checklist

Generated by Task 33. Each subsequent Phase 7 sub-task picks a section and migrates everything under it.

---

## Phase 7 sub-task assignments

| File / area | Phase 7 sub-task |
| --- | --- |
| `frontend/src/App.tsx` — Nav, SignInDropdown, UsernamePrompt, ErrorBoundary, root container | Task 34 |
| `frontend/src/pages/RoadmapTool/components/ScenarioPicker.tsx` | Task 35 |
| `frontend/src/pages/RoadmapTool/components/AddScenarioModal.tsx` | Task 35 |
| `frontend/src/pages/RoadmapTool/components/spend/SpendPanel.tsx` | Task 36 |
| `frontend/src/pages/RoadmapTool/components/spend/SpendTabContent.tsx` | Task 36 |
| `frontend/src/pages/RoadmapTool/components/summary/WalletSummaryStats.tsx` | Task 37 |
| `frontend/src/pages/RoadmapTool/components/summary/MethodologyInfoPopover.tsx` | Task 37 |
| `frontend/src/pages/RoadmapTool/components/summary/CurrencySettingsDropdown.tsx` | Task 37 |
| `frontend/src/pages/RoadmapTool/components/summary/WalletPortalSharesEditor.tsx` | Task 37 |
| `frontend/src/pages/RoadmapTool/components/timeline/WalletTimelineChart.tsx` | Task 38 (retokenize + IssuerRuleBanner) |
| `frontend/src/pages/RoadmapTool/components/ApplicationRuleWarningModal.tsx` | Task 39 |
| `frontend/src/pages/RoadmapTool/index.tsx` | Task 40 |

---

## Deprecated components to delete at Phase 7 completion

Both files are `@deprecated` and all import sites must be migrated before deletion:

- `frontend/src/components/ModalBackdrop.tsx` — import sites within scope:
  - `frontend/src/pages/RoadmapTool/components/AddScenarioModal.tsx` (L3, L35, L124)
  - `frontend/src/pages/RoadmapTool/components/ApplicationRuleWarningModal.tsx` (L2, L11, L54)
  - Out-of-scope import sites (migrated by separate tasks): `components/cards/WalletCardModal.tsx`, `components/cards/DeleteCardWarningModal.tsx`
- `frontend/src/components/InfoPopover.tsx` — exports `InfoIconButton` and `InfoQuoteBox`; import sites within scope:
  - `frontend/src/pages/RoadmapTool/index.tsx` (L42)
  - `frontend/src/pages/RoadmapTool/components/spend/SpendTabContent.tsx` (L13)
  - `frontend/src/pages/RoadmapTool/components/summary/WalletSummaryStats.tsx` (L5)
  - `frontend/src/pages/RoadmapTool/components/summary/MethodologyInfoPopover.tsx` (L1)
  - `frontend/src/pages/RoadmapTool/components/summary/CurrencySettingsDropdown.tsx` (L3)
  - `frontend/src/pages/RoadmapTool/components/summary/WalletPortalSharesEditor.tsx` (L8)
  - `frontend/src/pages/RoadmapTool/components/timeline/WalletTimelineChart.tsx` (L18)
  - Out-of-scope import sites (Profile, migrated separately): `pages/Profile/components/SpendingTab.tsx`

---

## `frontend/src/App.tsx`

**In-scope sections:** `ErrorBoundary` (L45–73), `SignInDropdown` (L79–254), `UsernamePrompt` (L256–306), `Nav` (L308–359), `App` root container (L383–435).
**Out of scope** (do not touch): `AuthGate`, `UsernameGate`, routing, `AuthProvider`, `QueryClientProvider` wrappers.

### Root container (L389)

- [ ] `<div className="h-dvh min-h-0 flex flex-col overflow-hidden bg-slate-950 text-slate-100">` → replace `bg-slate-950` with `bg-page` and `text-slate-100` with `text-ink`. The layout classes `h-dvh min-h-0 flex flex-col overflow-hidden` stay untouched; RoadmapTool depends on them.

### `Nav` component (L308–359)

- [ ] `<nav className="bg-slate-900 border-b border-slate-700 px-6 py-2.5 flex items-center gap-2">` → replace `bg-slate-900` with `bg-surface`, `border-slate-700` with `border-divider`. Keep spacing/layout classes.
- [ ] Brand link `className="text-white font-bold text-lg mr-6 hover:text-slate-200 transition-colors"` → replace `text-white` with `text-ink`, `hover:text-slate-200` with `hover:text-ink-muted`.
- [ ] Active NavLink class `'text-white bg-slate-800'` → replace `text-white` with `text-ink`, `bg-slate-800` with `bg-surface-2`.
- [ ] Inactive NavLink class `'text-slate-300 hover:text-white hover:bg-slate-800'` → replace `text-slate-300` with `text-ink-muted`, `hover:text-white` with `hover:text-ink`, `hover:bg-slate-800` with `hover:bg-surface-2`.
- [ ] Profile link `className="flex items-center gap-2 px-3 py-1 rounded-full text-slate-300 hover:text-white hover:bg-slate-800 transition-colors -mr-2"` → `text-slate-300` → `text-ink-muted`, `hover:text-white` → `hover:text-ink`, `hover:bg-slate-800` → `hover:bg-surface-2`.
- [ ] Sign-out button `className="text-sm font-medium px-5 py-2 rounded-full text-slate-300 hover:text-white hover:bg-slate-800 transition-colors ml-2"` → migrate to `<Button variant="ghost" size="sm">` (or apply token swaps if size doesn't match; the `rounded-full` shape override may need `className` passthrough).
- [ ] Add `<ThemeToggle />` from `components/ui/ThemeToggle` to the right of the auth area in the nav, before `<SignInDropdown />`.

### `SignInDropdown` component (L79–254)

- [ ] Trigger button `className="text-sm font-medium px-5 py-2 rounded-full text-slate-300 hover:text-white hover:bg-slate-800 transition-colors"` → `<Button variant="ghost" size="sm">` or token-swap: `text-slate-300` → `text-ink-muted`, `hover:text-white` → `hover:text-ink`, `hover:bg-slate-800` → `hover:bg-surface-2`.
- [ ] Dropdown panel `<div className="absolute right-0 mt-2 w-72 bg-slate-800 border border-slate-700 rounded-xl shadow-xl z-50">` → `bg-slate-800` → `bg-surface`, `border-slate-700` → `border-divider`.
- [ ] Tab strip (`flex border-b border-slate-700`): consider `<Tabs>` from `components/ui/Tabs`. Current implementation is a flex row with two buttons and an active indicator. `<Tabs>` accepts `items`, `active`, `onChange` — can directly replace this if the visual result is acceptable. If the tab strip visual needs the same `border-b-2 border-indigo-500` style, check whether `Tabs` renders that internally.
  - Active tab class `'text-white border-b-2 border-indigo-500'` → `text-ink border-b-2 border-accent`.
  - Inactive tab class `'text-slate-400 hover:text-slate-200'` → `text-ink-muted hover:text-ink`.
  - Separator `border-b border-slate-700` → `border-divider`.
- [ ] `inputClass` (L152–153): `'w-full bg-slate-900 border border-slate-600 rounded-lg px-3 py-2 text-sm text-white placeholder-slate-500 focus:outline-none focus:border-indigo-500'` → replace all three `<input>` elements (username, email, identifier, password) with `<Input>` from `components/ui/Input`, optionally wrapped in `<Field>` with `label` prop.
  - `bg-slate-900` → `bg-page`, `border-slate-600` → `border-divider`, `text-white` → `text-ink`, `placeholder-slate-500` → handled by `Input` internals, `focus:border-indigo-500` → `focus:border-accent`.
- [ ] Error paragraph `<p className="text-red-400 text-xs">` → `text-red-400` → `text-neg`.
- [ ] Submit button `className="w-full text-sm font-medium py-2 rounded-lg bg-indigo-600 hover:bg-indigo-500 disabled:opacity-50 text-white transition-colors"` → `<Button variant="primary" className="w-full">`.
- [ ] Divider spans `border-t border-slate-700` (×2) → `border-divider`.
- [ ] Divider text `className="text-slate-500 text-xs"` → `text-ink-faint`.

### `UsernamePrompt` component (L256–306)

- [ ] Outer overlay `<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60">` → replace with `<Modal open={true} onClose={() => undefined}>`  (no-op close since it's a forced gate). The backdrop and centering are handled by `Modal`.
- [ ] Inner panel `<div className="bg-slate-800 border border-slate-700 rounded-xl shadow-2xl p-6 w-80">` → becomes `<ModalBody>` content; the `bg-slate-800 border border-slate-700 rounded-xl` styles transfer to the `Modal` container itself (Modal already applies surface styling — verify and remove these classes from the inner div or let Modal's default styling take over). The `w-80` constraint may need `className="max-w-xs"` on `Modal`.
- [ ] `<h2 className="text-lg font-bold text-white mb-1">Choose a username</h2>` → `<ModalHeader><Heading level={3}>Choose a username</Heading></ModalHeader>`. `text-white` → `text-ink` if the Heading doesn't handle it.
- [ ] `<p className="text-slate-400 text-sm mb-4">` → `text-slate-400` → `text-ink-muted`.
- [ ] `<input>` for username → `<Field label="Username"><Input ... /></Field>` (or just `<Input>` if no label). Class: `"w-full bg-slate-900 border border-slate-600 rounded-lg px-3 py-2 text-sm text-white placeholder-slate-500 focus:outline-none focus:border-indigo-500"` → all handled by `Input` primitive.
- [ ] Error paragraph `<p className="text-red-400 text-xs">` → `text-neg`.
- [ ] Submit button `className="w-full text-sm font-medium py-2 rounded-lg bg-indigo-600 hover:bg-indigo-500 disabled:opacity-50 text-white transition-colors"` → `<Button variant="primary" className="w-full">`.

### `ErrorBoundary` component (L45–73)

- [ ] Container `<div className="p-8 text-center">` — no token changes needed here (no raw slate/indigo colors).
- [ ] `<h2 className="text-xl font-bold text-red-400 mb-2">` → `text-red-400` → `text-neg`. Consider `<Heading level={2}>`.
- [ ] `<p className="text-slate-400 text-sm mb-4">` → `text-slate-400` → `text-ink-muted`.
- [ ] Try-again button `className="px-4 py-2 bg-slate-700 rounded text-sm hover:bg-slate-600"` → `<Button variant="secondary">`.

---

## `frontend/src/pages/RoadmapTool/components/ScenarioPicker.tsx`

**Lines that matter:** L49–84 (trigger button), L85–172 (dropdown panel, scenario rows, add button).

### Trigger button (L49–55)

- [ ] `className="flex items-center gap-1.5 px-3 py-1 rounded-md bg-slate-800 hover:bg-slate-700 border border-slate-700 text-slate-200 text-sm font-medium transition-colors"` →
  - `bg-slate-800` → `bg-surface`
  - `hover:bg-slate-700` → `hover:bg-surface-2`
  - `border-slate-700` → `border-divider`
  - `text-slate-200` → `text-ink`
  - Consider wrapping in `<Button variant="secondary" size="sm">` with the gap/truncate content inside; the `flex items-center gap-1.5` layout can be the `Button`'s children.

### Dropdown panel (L86–89)

- [ ] `className="absolute top-full mt-1 left-0 z-50 min-w-[18rem] max-h-72 overflow-auto rounded-md bg-slate-800 border border-slate-700 shadow-lg py-1"` →
  - `bg-slate-800` → `bg-surface`
  - `border-slate-700` → `border-divider`

### Scenario rows (L90–153)

- [ ] Active scenario class `'text-indigo-300 font-semibold'` → `text-accent font-semibold`.
- [ ] Inactive scenario class `'text-slate-200'` → `text-ink`.
- [ ] Row hover `hover:bg-slate-700` → `hover:bg-surface-2`.
- [ ] "Default" metadata button `className="opacity-0 group-hover:opacity-100 px-1.5 py-0.5 text-[10px] uppercase tracking-wide text-amber-300 hover:bg-amber-500/10 rounded transition-opacity"` → `text-amber-300` → `text-warn` (or keep amber since it's a specific warning-adjacent affordance; flag as open question if `text-warn` is amber). The `text-[10px] uppercase tracking-wide` pattern is the `<Eyebrow>` shape — consider `<Eyebrow>` inside the button if the button structure permits.
- [ ] Delete button `className="opacity-0 group-hover:opacity-100 p-1 text-slate-500 hover:text-red-400 rounded transition-opacity"` → `text-slate-500` → `text-ink-faint`, `hover:text-red-400` → `hover:text-neg`.
- [ ] Divider `border-t border-slate-700/60` → `border-divider` (with opacity tweak: `border-divider/60` if the token supports opacity syntax, otherwise keep as-is and note as an open question).

### "New scenario" add button (L155–168)

- [ ] `className="flex items-center gap-2 w-full text-left px-3 py-1.5 text-sm text-indigo-300 hover:bg-slate-700 transition-colors"` →
  - `text-indigo-300` → `text-accent`
  - `hover:bg-slate-700` → `hover:bg-surface-2`

---

## `frontend/src/pages/RoadmapTool/components/AddScenarioModal.tsx`

**Lines that matter:** L3 (`ModalBackdrop` import), L35–38 (`ModalBackdrop` usage + container class), L52–53 heading + label, L56–65 inputs, L83–98 select, L101 error, L103–122 footer buttons.

### Modal container (L35–39)

- [ ] Remove `import { ModalBackdrop } from '../../../components/ModalBackdrop'` (L3).
- [ ] Replace `<ModalBackdrop onClose={...} label="Add scenario" className="bg-slate-900 border border-slate-700 rounded-xl shadow-xl w-full max-w-md p-5">` with:
  ```tsx
  <Modal open={true} onClose={isSubmitting ? () => undefined : onClose} size="md">
    <ModalHeader><Heading level={3}>New Scenario</Heading></ModalHeader>
    <ModalBody>...</ModalBody>
    <ModalFooter>...</ModalFooter>
  </Modal>
  ```
  - `bg-slate-900 border border-slate-700 rounded-xl` → handled by `Modal` default surface styling; drop from explicit className.

### Heading (L52)

- [ ] `<h2 className="text-lg font-semibold text-slate-100 mb-3">New Scenario</h2>` → move into `<ModalHeader><Heading level={3}>New Scenario</Heading></ModalHeader>`. Token: `text-slate-100` → `text-ink` (handled by `Heading` internally).

### Field labels (L53–54, L66–67, L80–81)

- [ ] `<label className="block text-xs font-medium text-slate-400 uppercase tracking-wider mb-1">Name</label>` → `<Eyebrow>Name</Eyebrow>` or use `<Field label="Name">` wrapping the input. `text-slate-400` → `text-ink-muted`.
- Same pattern for "Description" label (L66–67) and "Copy from (optional)" label (L80–81).

### Text inputs (L56–64, L69–76)

- [ ] Name `<input className="w-full px-3 py-2 rounded-md bg-slate-800 border border-slate-700 text-slate-100 placeholder-slate-500 focus:outline-none focus:ring-2 focus:ring-indigo-500/50 focus:border-indigo-500">` → `<Input>` from `components/ui/Input`. Token swaps if not using the component: `bg-slate-800` → `bg-surface`, `border-slate-700` → `border-divider`, `text-slate-100` → `text-ink`, `focus:border-indigo-500` → `focus:border-accent`.
- [ ] Description input (L69–76) — same swap as Name input.

### Select (L83–98)

- [ ] `<select className="w-full px-3 py-2 rounded-md bg-slate-800 border border-slate-700 text-slate-100 focus:outline-none focus:ring-2 focus:ring-indigo-500/50 focus:border-indigo-500">` → `<Select>` from `components/ui/Select`. Same token swaps as inputs.

### Error message (L101)

- [ ] `<p className="mt-2 text-sm text-red-400">` → `text-red-400` → `text-neg`.

### Footer buttons (L103–122)

- [ ] Cancel button `className="px-3 py-1.5 rounded-md text-sm text-slate-300 hover:bg-slate-800 transition-colors disabled:opacity-50"` → `<Button variant="ghost" size="sm">`. Token swaps: `text-slate-300` → `text-ink-muted`, `hover:bg-slate-800` → `hover:bg-surface`.
- [ ] Submit button (enabled state) `className="... bg-indigo-500 hover:bg-indigo-400 text-white"` → `<Button variant="primary" size="sm">`.
- [ ] Submit button (disabled state) `className="... bg-slate-700 text-slate-500 cursor-not-allowed"` → handled by `Button`'s `disabled` prop (built-in `disabled:opacity-50`); no separate class needed.
- [ ] Move footer buttons to `<ModalFooter>`.

---

## `frontend/src/pages/RoadmapTool/components/spend/SpendPanel.tsx`

**Lines that matter:** L21 (outer panel div).

- [ ] `<div className="bg-slate-900 border border-slate-700 rounded-xl p-4 min-w-0 min-h-0 h-full flex flex-col overflow-hidden">` → `<Surface variant="panel" padding="none" className="min-w-0 min-h-0 h-full flex flex-col overflow-hidden p-4">`. Token swaps: `bg-slate-900` → `bg-surface`, `border-slate-700` → `border-divider`, `rounded-xl` → handled by `Surface`. Alternatively, keep as `<div>` and just swap `bg-slate-900` → `bg-surface`, `border-slate-700` → `border-divider`.

---

## `frontend/src/pages/RoadmapTool/components/spend/SpendTabContent.tsx`

**Lines that matter:** L13 (InfoPopover import), L501 (table container), L503–506 (calculate prompt banner), L514 (thead bg), L534 (total row bg), L590–615 (info icon button inline), L660–689 (tag badges), L702–735 (InfoQuoteBox).

### Deprecated imports (L13)

- [ ] Remove `import { InfoQuoteBox } from '../../../../components/InfoPopover'`.
- [ ] Replace with `import { Popover } from '../../../../components/ui/Popover'`.

### Table container (L501)

- [ ] `<div className="flex-1 min-h-0 overflow-auto rounded-lg border border-slate-800">` → `border-slate-800` → `border-divider`.

### Calculate prompt banner (L503–506)

- [ ] `<div className="px-3 py-2 text-xs text-indigo-200 bg-indigo-900/20 border-b border-indigo-700/40">` → `text-indigo-200` → `text-accent` (or consider `text-info`; indigo-200 is used here for a call-to-action, which maps better to `text-accent`), `border-indigo-700/40` → `border-accent/40`. The `bg-indigo-900/20` stays as-is or becomes `bg-accent/10`.
- [ ] Inner `<span className="font-semibold text-indigo-300">Calculate</span>` → `text-indigo-300` → `text-accent`.

### Table header (L514–531)

- [ ] `<thead className="sticky top-0 bg-slate-900 z-10">` → `bg-slate-900` → `bg-surface`.
- [ ] All `<th className="... text-slate-300 ... border-b border-r border-slate-800">` → `text-slate-300` → `text-ink-muted`, `border-slate-800` → `border-divider`.

### Total row (L534–575)

- [ ] `<tr className="border-b-2 border-slate-700 bg-slate-800/50">` → `border-slate-700` → `border-divider`, `bg-slate-800/50` → `bg-surface/50` (or `bg-surface-2/50`).
- [ ] `<th className="text-left px-3 py-2 text-slate-100 font-semibold border-r border-slate-800/60">` → `text-slate-100` → `text-ink`, `border-slate-800/60` → `border-divider/60`.
- [ ] Total spend `<div className="text-slate-100 font-semibold">` → `text-slate-100` → `text-ink`.
- [ ] Cycle prev/next buttons `className="shrink-0 p-0.5 rounded text-slate-500 hover:text-slate-200 hover:bg-slate-800 ..."` → `text-slate-500` → `text-ink-faint`, `hover:text-slate-200` → `hover:text-ink`, `hover:bg-slate-800` → `hover:bg-surface-2`. These are icon buttons; consider `<Button variant="icon" size="sm">`.

### Table body rows (L583–696)

- [ ] Row border `<tr className="border-b border-slate-800/60">` → `border-slate-800/60` → `border-divider/60`.
- [ ] Category cell `<td className="... text-slate-200 border-r border-slate-800/60">` → `text-slate-200` → `text-ink`, `border-slate-800/60` → `border-divider/60`.
- [ ] Info icon button (L593–614): currently a raw `<button>` with an SVG. Replace with `<Popover>` from `components/ui/Popover`. The Popover uses a render-prop `trigger` that receives `{ onClick, ref }` — the existing `<button>` becomes the trigger element. The `<InfoQuoteBox>` content moves into the Popover's `children`.
  - Active button class `'text-indigo-300 bg-indigo-500/10'` → `text-accent bg-accent/10`.
  - Inactive button class `'text-slate-500 hover:text-slate-300 hover:bg-slate-700/50'` → `text-ink-faint hover:text-ink-muted hover:bg-surface-2/50`.
- [ ] Earn cell `<td className="... text-slate-200 border-r ...">` → `text-slate-200` → `text-ink`, `border-r border-slate-800/60` → `border-divider/60`.
- [ ] Placeholder dash `<span className="text-slate-700">—</span>` → `text-slate-700` → `text-ink-faint`.
- [ ] Card earn column `<td className="... text-slate-200 ...">` → `text-ink`.
- [ ] Card name text `<div className="text-xs text-slate-200 truncate mb-0.5">` → `text-ink`.

### Tag / pill badges (L663–688) — **High-priority `<Badge>` candidates**

- [ ] Portal badge `<span className="text-[10px] uppercase tracking-wide text-amber-300/90 bg-amber-500/10 border border-amber-500/30 rounded px-1 py-[1px]">Portal</span>` → `<Badge tone="warn">Portal</Badge>`. `text-amber-300` → `text-warn`.
- [ ] Rotating badge `<span className="text-[10px] uppercase tracking-wide text-violet-300/90 bg-violet-500/10 border border-violet-500/30 rounded px-1 py-[1px]">Rotating</span>` → `<Badge tone="info">Rotating</Badge>` (violet is closest to info). Flag as open question if violet doesn't map cleanly to `text-info` (blue/sky).
- [ ] Override badge `<span className="text-[10px] uppercase tracking-wide text-emerald-300/90 bg-emerald-500/10 border border-emerald-500/30 rounded px-1 py-[1px]">Override</span>` → `<Badge tone="pos">Override</Badge>`. `text-emerald-300` → `text-pos`.

### ROS percentage (L660)

- [ ] `<div className="text-xs font-semibold text-indigo-300 tabular-nums">` → `text-indigo-300` → `text-accent`.

### InfoQuoteBox → Popover (L702–735)

- [ ] `{infoCategory && <InfoQuoteBox anchorEl={infoCategory.anchor} title={...} onClose={() => setInfoCategory(null)}>...</InfoQuoteBox>}` → the Popover architecture requires the trigger to own the `<Popover>` wrapper. Move the Popover up to wrap the info `<button>` at L593. The current flow of: click → `setInfoCategory({ cat, anchor })` → render `InfoQuoteBox` outside the row needs to be refactored so the `<Popover trigger={...}>` is colocated with the button (one per row), and the category data is captured in closure. No global `infoCategory` state needed. Content inside `<InfoQuoteBox>` (the `<ul>` of mappings) becomes Popover `children`.
  - Inner `<p className="text-slate-300 font-medium mb-1.5">` → `text-slate-300` → `text-ink-muted`.
  - `<span className="text-slate-300">` → `text-ink-muted`.
  - `<span className="text-slate-500 tabular-nums">` → `text-ink-faint`.
  - Footer hint `<p className="text-xs text-slate-500 mt-2">` → `text-ink-faint`.

---

## `frontend/src/pages/RoadmapTool/components/summary/WalletSummaryStats.tsx`

**Lines that matter:** L5 (InfoPopover import), L82–83 (panel border logic), L88–183 (left stats panel), L187–242 (duration panel), L247–294 (include SUBs toggle), L297–341 (legend panel), L343–488 (five InfoQuoteBox popovers).

### Deprecated imports (L5)

- [ ] Remove `import { InfoIconButton, InfoQuoteBox } from '../../../../components/InfoPopover'`.
- [ ] Add `import { Popover } from '../../../../components/ui/Popover'` (replaces all InfoIconButton + InfoQuoteBox usages).

### Panel borders and panel backgrounds (L88, L187, L247, L297)

- [ ] All four panels share the pattern `className={`... bg-slate-900 border rounded-xl ... ${panelBorder}`}` where `panelBorder` is `'border-amber-700/60'` or `'border-slate-700'`.
  - `bg-slate-900` → `bg-surface`
  - `border-slate-700` (base) → `border-divider`
  - Keep `border-amber-700/60` stale-state class as-is (it's a dynamic semantic color, not a token to swap).
  - Consider replacing each panel `<div>` with `<Surface variant="panel">` (keep `padding="none"` and add the existing spacing via `className`).

### Stats panel eyebrow labels (L112, L135, L158)

- [ ] `<p className="text-[10px] text-indigo-300 uppercase tracking-wider whitespace-nowrap">Effective Annual Fee</p>` → `<Eyebrow accent>Effective Annual Fee</Eyebrow>`. `text-indigo-300` → `text-accent`.
- [ ] `<p className="text-[10px] text-slate-400 uppercase tracking-wider whitespace-nowrap">Annual Fees</p>` → `<Eyebrow>Annual Fees</Eyebrow>`. `text-slate-400` → `text-ink-muted`.
- [ ] `<p className="text-[10px] text-slate-400 uppercase tracking-wider whitespace-nowrap">Point Income</p>` → `<Eyebrow>Point Income</Eyebrow>`.

### Stat values (L124–128, L147–152, L170–175)

- [ ] EAF value `<p className={`text-xl font-bold tabular-nums truncate ${totalEffectiveAF < 0 ? 'text-emerald-400' : 'text-indigo-100'}`}>` → `text-emerald-400` → `text-pos`, `text-indigo-100` → `text-ink`. Consider `<Money value={totalEffectiveAF} tone="auto">` from `components/ui/Money` (tone="auto" applies pos/neg automatically).
- [ ] Annual fees value `<p className="text-xl font-bold text-white tabular-nums truncate">` → `text-white` → `text-ink`.
- [ ] Point income value `<p className="text-xl font-bold text-white tabular-nums truncate">` + `<span className="ml-1 text-sm font-medium text-slate-400">Pts/Year</span>` → `text-white` → `text-ink`, `text-slate-400` → `text-ink-muted`.
- [ ] Dividers `<div className="bg-slate-700/60 self-stretch my-1" />` → `bg-slate-700/60` → `bg-divider/60`.

### InfoIconButton instances — replace with Popover (L113–122, L136–145, L159–168, L192–201, L251–260)

All five `<InfoIconButton>` usages follow the same pattern: click → set `statTopic` state → render `<InfoQuoteBox>` outside the grid. Under the new Popover primitive the trigger and content are colocated:

```tsx
<Popover
  trigger={({ onClick, ref }) => (
    <Button variant="icon" size="sm" onClick={onClick} ref={ref} aria-label="...">
      {/* ⓘ icon SVG */}
    </Button>
  )}
>
  {/* InfoQuoteBox content here */}
</Popover>
```

The `statTopic` state and the five separate `{statTopic?.name === 'eaf' && <InfoQuoteBox ...>}` blocks at L343–488 can all be removed once the Popovers are colocated.

- [ ] Migrate EAF `<InfoIconButton>` (L113–122) → inline `<Popover>` containing the EAF explanation (currently L343–369).
- [ ] Migrate Annual Fees `<InfoIconButton>` (L136–145) → inline `<Popover>` containing fees explanation (currently L401–425).
- [ ] Migrate Point Income `<InfoIconButton>` (L159–168) → inline `<Popover>` containing income explanation (currently L372–397).
- [ ] Migrate Time Horizon `<InfoIconButton>` (L192–201) → inline `<Popover>` containing duration explanation (currently L463–488).
- [ ] Migrate Sign-Up Bonuses `<InfoIconButton>` (L251–260) → inline `<Popover>` containing SUBs explanation (currently L428–460).
- [ ] Remove `statTopic` state and all five `{statTopic?.name === ... && <InfoQuoteBox>}` blocks (L49, L343–488).

### Duration panel (L187–242)

- [ ] Panel heading `<span className="text-[10px] text-slate-400 uppercase tracking-wider">Time Horizon</span>` → `<Eyebrow>Time Horizon</Eyebrow>`. `text-slate-400` → `text-ink-muted`.
- [ ] Duration display `<span className="text-xs font-medium text-slate-200 tabular-nums">` → `text-slate-200` → `text-ink`.
- [ ] Range input `className="w-full h-1.5 accent-indigo-500 cursor-pointer block my-0"` → `accent-indigo-500` → `accent-accent`.
- [ ] Tick labels `<span className={`absolute text-[10px] text-slate-500 tabular-nums ...`}` → `text-slate-500` → `text-ink-faint`.

### Include SUBs toggle panel (L247–294)

- [ ] Panel label `<span className="text-[10px] text-slate-400 uppercase tracking-wider whitespace-nowrap">Sign-Up Bonuses</span>` → `<Eyebrow>Sign-Up Bonuses</Eyebrow>`.
- [ ] Segmented control container `<div className="flex rounded-md border border-slate-700 overflow-hidden text-xs font-medium">` → `border-slate-700` → `border-divider`.
- [ ] Active button `className={`flex-1 px-2 py-1 transition-colors ${includeSubs ? 'bg-indigo-500/90 text-white' : 'bg-slate-800 text-slate-400 hover:text-slate-200'}`}` → `bg-indigo-500/90` → `bg-accent/90`, `text-white` stays, inactive: `bg-slate-800` → `bg-surface`, `text-slate-400` → `text-ink-muted`, `hover:text-slate-200` → `hover:text-ink`.
- [ ] Second button border `border-l border-slate-700` → `border-divider`.
- [ ] **Note**: This segmented control could be replaced with `<Tabs>` (pass `items=[{key:'include', label:'Include'},{key:'exclude', label:'Exclude'}]`, `active={includeSubs ? 'include' : 'exclude'}`, `onChange`). Verify `<Tabs>` renders the same visual shape; if not, keep as-is with token swaps only.

### Legend panel (L297–341)

- [ ] Panel label items `<div className="grid grid-cols-[26px_1fr] gap-x-2 gap-y-1.5 items-center text-[11px] text-slate-500">` → `text-slate-500` → `text-ink-faint`.
- [ ] Color swatches use inline `style={{ backgroundColor, border }}` with hardcoded hex values (e.g. `#fbbf2433`, `#fbbf24`, `#818cf833`, `#818cf8`) — these are chart-specific semantic colors, not design-system token candidates. Keep as-is with hardcoded values (flag in Open Questions that these could become CSS vars if a color palette is standardized).

### InfoQuoteBox content classes (inside the five popovers L343–488)

Once the InfoQuoteBox content moves inside Popover `children`, token-swap the inline text classes:
- [ ] All `<p className="text-slate-300 font-medium mb-1">` headings → `text-slate-300` → `text-ink-muted`.
- [ ] Body paragraph `<p>` (unclassed, inherits from InfoQuoteBox) — ensure Popover content inherits `text-ink-muted` or similar readable color.

### Error state (L91–93)

- [ ] `<div className="text-red-400 text-sm bg-red-950 border border-red-700 rounded-lg p-3">` → `text-red-400` → `text-neg`, `bg-red-950` → keep as `bg-red-950` (no direct token; flag as open question), `border-red-700` → `border-neg` (if that token exists; otherwise keep `border-red-700`).

### Empty state text (L95–103)

- [ ] `<div className="text-slate-300 text-xs text-center py-2">` → `text-slate-300` → `text-ink-muted`.
- [ ] `<span className="font-semibold text-indigo-300">Calculate</span>` → `text-indigo-300` → `text-accent`.
- [ ] `<div className="text-slate-500 text-xs text-center py-2">` → `text-slate-500` → `text-ink-faint`.

---

## `frontend/src/pages/RoadmapTool/components/summary/MethodologyInfoPopover.tsx`

**Lines that matter:** L1 (import), L10 (InfoQuoteBox usage).

This is a thin wrapper around a single `<InfoQuoteBox>`. The component itself can likely be deleted entirely after migration — its content moves inline as a `<Popover>` at the call site in `index.tsx`.

If kept as a component:

- [ ] Remove `import { InfoQuoteBox } from '../../../../components/InfoPopover'` (L1).
- [ ] Change props from `{ anchorEl, onClose }` (old InfoQuoteBox API) to accept a Popover render-prop pattern, or simply inline the content in `index.tsx` and delete this file.
- [ ] Inner paragraph `<p className="text-slate-300 font-medium mb-1">` → `text-slate-300` → `text-ink-muted`.

---

## `frontend/src/pages/RoadmapTool/components/summary/CurrencySettingsDropdown.tsx`

**Lines that matter:** L3 (InfoPopover import), L71–73 (container div), L80–99 (CPP settings row), L131–168 (InfoQuoteBox popover for no-transfer info).

### Deprecated imports (L3)

- [ ] Remove `import { InfoIconButton, InfoQuoteBox } from '../../../../components/InfoPopover'`.
- [ ] Add `import { Popover } from '../../../../components/ui/Popover'`.

### Container (L71–73)

- [ ] `<div className="relative z-20 bg-slate-800/70 border-b border-slate-700 px-5 pt-2 pb-4" style={{...}}>` → `bg-slate-800/70` → `bg-surface/70`, `border-slate-700` → `border-divider`.

### CPP settings row (L80–99)

- [ ] Label `<div className="flex items-center justify-between text-[11px] text-slate-300 mb-1.5">` → `text-slate-300` → `text-ink-muted`.
- [ ] Eyebrow label `<span className="text-slate-400 uppercase tracking-wider">Cents Per Point</span>` → `<Eyebrow>Cents Per Point</Eyebrow>`. `text-slate-400` → `text-ink-muted`.
- [ ] CPP value `<span className="text-indigo-300 tabular-nums">{clampedCpp.toFixed(2)}¢</span>` → `text-indigo-300` → `text-accent`.
- [ ] Range input `className="w-full h-1.5 accent-indigo-500 cursor-pointer block my-0"` → `accent-indigo-500` → `accent-accent`.
- [ ] `<InfoIconButton>` (L86–94) → replace with `<Popover trigger={...}>` wrapping the "Without a premium card" info button. The existing `noTransferAnchor` state can be removed; the Popover manages its own open state.

### NoTransfer InfoQuoteBox → Popover (L131–168)

- [ ] `{noTransferAnchor && <InfoQuoteBox anchorEl={noTransferAnchor} ...>...</InfoQuoteBox>}` → content moves into the `<Popover>` children from the trigger at L86–94.
- [ ] `<span className="text-amber-300 font-medium">` (×2) → `text-amber-300` → `text-warn`.
- [ ] Remove `noTransferAnchor` state (L35) once Popover is colocated.

### Loading state (L75)

- [ ] `<div className="text-slate-500 text-sm">Loading…</div>` → `text-slate-500` → `text-ink-faint`.

---

## `frontend/src/pages/RoadmapTool/components/summary/WalletPortalSharesEditor.tsx`

**Lines that matter:** L8 (InfoPopover import), L111–128 (portal row header with InfoIconButton), L131–161 (range input), L169–183 (InfoQuoteBox).

### Deprecated imports (L8)

- [ ] Remove `import { InfoIconButton, InfoQuoteBox } from '../../../../components/InfoPopover'`.
- [ ] Add `import { Popover } from '../../../../components/ui/Popover'`.

### Portal row header (L111–128)

- [ ] `<div className="flex items-center justify-between text-[11px] text-slate-300 mb-1.5">` → `text-slate-300` → `text-ink-muted`.
- [ ] `<span className="text-slate-400 uppercase tracking-wider">Travel Portal Spend</span>` → `<Eyebrow>Travel Portal Spend</Eyebrow>`. `text-slate-400` → `text-ink-muted`.
- [ ] Portal name `<span className="text-indigo-300 tabular-nums">{pct}%</span>` → `text-indigo-300` → `text-accent`.
- [ ] `<InfoIconButton>` (L116–124) → replace with colocated `<Popover trigger={...}>`. The existing `infoAnchor` state and the `{infoAnchor && <InfoQuoteBox>}` block (L168–183) can be eliminated; Popover manages open/close internally.

### Range input (L131–161)

- [ ] `className="w-full h-1.5 accent-indigo-500 cursor-pointer block my-0"` → `accent-indigo-500` → `accent-accent`.

### InfoQuoteBox → Popover (L169–183)

- [ ] Move content inside the Popover triggered by the `<InfoIconButton>` at L116.
- [ ] Remove `infoAnchor` state (L98).

---

## `frontend/src/pages/RoadmapTool/components/timeline/WalletTimelineChart.tsx`

**Note:** This is the most complex file. Task 38 is **retokenize + IssuerRuleBanner only** — no structural refactor. The chart's precise positioning/layering uses hardcoded pixel values and inline `style` props that must not be changed. Limit changes to Tailwind class token swaps and the issuer-rule panel.

### Deprecated imports (L18)

- [ ] Remove `import { InfoQuoteBox } from '../../../../components/InfoPopover'`.
- [ ] Add `import { Popover } from '../../../../components/ui/Popover'` (for the rules popover at L779–845).
- [ ] Add `import { IssuerRuleBanner } from '../../../../components/cards/IssuerRuleBanner'` if that component directly covers the rules list display (check IssuerRuleBanner's API before deciding).

### Root panel (L591)

- [ ] `<div className="bg-slate-900 border border-slate-700 rounded-xl pt-2 px-4 pb-4 min-w-0 min-h-0 h-full flex flex-col overflow-hidden">` → `bg-slate-900` → `bg-surface`, `border-slate-700` → `border-divider`.

### Empty state (L594–608)

- [ ] `<p className="text-slate-500 text-sm">No cards yet.</p>` → `text-slate-500` → `text-ink-faint`.
- [ ] "Add Card" button `className="flex items-center gap-1.5 px-4 py-2 rounded-lg text-sm font-medium bg-indigo-600 hover:bg-indigo-500 text-white transition-colors"` → `<Button variant="primary">Add Card</Button>`. `bg-indigo-600` → `bg-accent`, `hover:bg-indigo-500` → `hover:bg-accent`.

### Axis header (L617–707)

- [ ] Left gutter `<div className="bg-slate-900 ${DIVIDER_CLASS} px-3 ...">` where `DIVIDER_CLASS = 'border-b border-slate-800'` (L44) → `bg-slate-900` → `bg-surface`, `border-slate-800` → `border-divider`. Update the `DIVIDER_CLASS` constant at L44 from `'border-b border-slate-800'` to `'border-b border-divider'`.
- [ ] `<h2 className="text-base font-semibold text-slate-100">Cards</h2>` → `text-slate-100` → `text-ink`. Consider `<Heading level={2}>`.
- [ ] "Add Card" header button `className="flex items-center gap-1 px-2 py-0.5 rounded-md text-xs font-medium bg-indigo-600 hover:bg-indigo-500 text-white transition-colors"` → `bg-indigo-600` → `bg-accent`, `hover:bg-indigo-500` → `hover:bg-accent`.
- [ ] Rules button (L643–669): severity-based color classes contain `text-red-400`, `text-amber-400`, `text-indigo-300`, `text-slate-500` → `text-neg`, `text-warn`, `text-accent`, `text-ink-faint` respectively. Active variants: `text-red-300` → `text-neg`, `text-amber-300` → `text-warn`, `text-indigo-300` → `text-accent`.
- [ ] Axis right `<div className="bg-slate-900 ${DIVIDER_CLASS} relative">` → `bg-surface`.
- [ ] Year tick labels `<div className="absolute ... text-[11px] text-slate-500">` → `text-slate-500` → `text-ink-faint`.
- [ ] "Today" / end-date labels `<div className="absolute ... text-[11px] text-slate-300 font-semibold">` → `text-slate-300` → `text-ink-muted`.

### Grid gridlines (L720–748)

- [ ] Year gridlines `<div className="absolute ... border-l border-slate-700">` → `border-slate-700` → `border-divider`.
- [ ] Today/end vertical bars use inline `style={{ backgroundColor: '#64748b' }}` — this is a hardcoded hex for `slate-500`. Replace with `bg-slate-500` as Tailwind or keep hardcoded (in-SVG/canvas positioning context; flag as open question).

### `GroupSection` component (L909–1033)

- [ ] Group header `<div className="... bg-slate-800" style={{...borderLeft: `3px solid ${group.color}`}}>` → `bg-slate-800` → `bg-surface-2`. The colored left border uses a dynamic runtime color (`group.color`) — keep as inline style.
- [ ] Group name `<div className="text-sm font-medium text-slate-200 truncate">` → `text-slate-200` → `text-ink`.
- [ ] Balance/income `<div className="... text-xs text-slate-400 truncate">` → `text-slate-400` → `text-ink-muted`.
- [ ] Secondary figures `<span className="text-slate-500">` → `text-ink-faint`.
- [ ] Separators `<span className="text-slate-600 text-sm leading-none">·</span>` and `<span className="mr-1 text-slate-700">·</span>` → `text-ink-faint`.
- [ ] Settings gear button active class `'bg-slate-700 text-indigo-300'` → `bg-surface text-accent`; inactive `'text-slate-500 hover:text-indigo-400 hover:bg-slate-700'` → `text-ink-faint hover:text-accent hover:bg-surface-2`.
- [ ] Right currency header cell `<div className="... bg-slate-800">` → `bg-surface-2`.

### `CardRow` component (L1050–1395)

- [ ] Left gutter `<div className="flex items-center gap-2 px-3 ${DIVIDER_CLASS} transition-colors group-hover:bg-slate-800/60 ...">` → `group-hover:bg-slate-800/60` → `group-hover:bg-surface-2/60`.
- [ ] Card name `<div className="text-sm font-medium text-slate-200 truncate">` → `text-ink`.
- [ ] Income label `<div className="text-xs text-slate-500 truncate ...">` → `text-ink-faint`.
- [ ] Housing fee `<span className="text-rose-400">−{...} Housing Fee</span>` → `text-rose-400` → `text-neg`.
- [ ] Lock icon `className="shrink-0 text-amber-400 ..."` → `text-amber-400` → `text-warn`.
- [ ] EAF label color classes (L1293–1298): `'text-slate-500'` → `text-ink-faint`, `'text-emerald-400'` → `text-pos`, `'text-red-400'` → `text-neg`, `'text-slate-200'` → `text-ink`.

### `IconHoverLabel` tooltip portal (L52–99)

- [ ] Tooltip div `className="pointer-events-none fixed z-[60] ... bg-slate-950 text-slate-100 border border-slate-700 rounded px-1.5 py-0.5 shadow-lg"` → `bg-slate-950` → `bg-page`, `text-slate-100` → `text-ink`, `border-slate-700` → `border-divider`.

### `CardThumb` fallback (L1418–1421)

- [ ] `<div className="w-14 h-9 rounded bg-slate-800 border border-slate-700 shrink-0" />` → `bg-slate-800` → `bg-surface`, `border-slate-700` → `border-divider`.

### `ToggleSwitch` component (L1463–1493)

- [ ] This is a custom toggle switch — replace with `<Toggle>` from `components/ui/Toggle`. The Toggle primitive is a styled `<input type="checkbox" role="switch">`. The current `<button role="switch">` can be replaced, but check that the API (`checked`, `onChange`, `disabled`) matches.
- [ ] Enabled class `'bg-indigo-500'` → `bg-accent`. Disabled class `'bg-slate-700'` → `bg-surface-2`.
- [ ] The `<span>` thumb uses inline `style={{ left: enabled ? 16 : 2 }}` — this is implementation detail that may disappear with the Toggle primitive.

### Application rules InfoQuoteBox → Popover + IssuerRuleBanner (L779–845)

- [ ] `{rulesAnchor && <InfoQuoteBox anchorEl={rulesAnchor} title="Application Rules" onClose={() => setRulesAnchor(null)}>...</InfoQuoteBox>}` → replace with a colocated `<Popover>` wrapping the rules button at L643.
- [ ] The rules list inside InfoQuoteBox renders severity-colored `<li>` items. Consider `<IssuerRuleBanner>` from `components/cards/IssuerRuleBanner` — check its API to see if it accepts individual `RoadmapRuleStatus` items with severity. If IssuerRuleBanner renders a self-contained row (not a list), one `<IssuerRuleBanner>` per rule in the `applicableRules.map(...)`.
  - `containerClass` using `border-red-700/50 bg-red-900/20` → flag as open question (no direct token for these; check if IssuerRuleBanner handles severity internally).
  - `titleClass` `'text-red-300'` → `text-neg`, `'text-amber-300'` → `text-warn`, `'text-slate-200'` → `text-ink`.
  - `intervalClass` `'text-red-300'` → `text-neg`, `'text-amber-300'` → `text-warn`, `'text-slate-400'` → `text-ink-muted`.
  - `<span className="text-[10px] text-slate-500 shrink-0">` (issuer name) → `text-ink-faint`.
  - `<p className="text-[11px] text-slate-400 mt-0.5">` (description) → `text-ink-muted`.
- [ ] Remove `rulesAnchor` state and `{rulesAnchor && ...}` block once Popover is colocated at the button.

---

## `frontend/src/pages/RoadmapTool/components/ApplicationRuleWarningModal.tsx`

**Lines that matter:** L2 (ModalBackdrop import), L11–54 (entire component).

### Modal migration (L2, L11, L54)

- [ ] Remove `import { ModalBackdrop } from '../../../components/ModalBackdrop'` (L2).
- [ ] Replace `<ModalBackdrop onClose={onClose} className="bg-slate-900 border border-amber-700/50 rounded-xl p-5 max-w-lg w-full shadow-xl">` with:
  ```tsx
  <Modal open={true} onClose={onClose} className="max-w-lg border-amber-700/50">
    <ModalHeader>
      <Heading level={3} className="text-warn">Application rule warning</Heading>
    </ModalHeader>
    <ModalBody>...</ModalBody>
    <ModalFooter>...</ModalFooter>
  </Modal>
  ```
  - The `border-amber-700/50` border override is a semantic color; keep it as a `className` override on `Modal` if Modal accepts a `className` prop for the panel.

### Title (L22–25)

- [ ] `<h2 className="text-base font-semibold text-amber-300 mb-1">Application rule warning</h2>` → `<Heading level={3}>Application rule warning</Heading>` inside `<ModalHeader>`. `text-amber-300` → `text-warn`.

### Description (L26–28)

- [ ] `<p className="text-xs text-slate-400 mb-4">` → `text-slate-400` → `text-ink-muted`.

### Violation items (L29–46) — **IssuerRuleBanner candidate**

- [ ] Each `<div key={r.rule_id} className="flex items-start gap-3 bg-amber-900/20 border border-amber-700/40 rounded-lg px-3 py-2">` violation row → `<IssuerRuleBanner>` from `components/cards/IssuerRuleBanner`. Check if the component accepts a `RoadmapRuleStatus` directly or if it needs the individual fields mapped. If it does accept the full object, the whole `violations.map(...)` becomes `{violations.map((r) => <IssuerRuleBanner key={r.rule_id} rule={r} />)}`.
  - If IssuerRuleBanner does not accept the API shape directly, apply token swaps: `bg-amber-900/20` and `border-amber-700/40` stay (no token; flag as open question), `text-amber-400 font-bold` (rule name) → `text-warn`, `text-amber-300 font-medium` (issuer name) → `text-warn`, `text-amber-400` (count) → `text-warn`, `text-slate-300` (description) → `text-ink-muted`.

### OK button (L46–51)

- [ ] `<button type="button" className="mt-5 w-full bg-slate-700 hover:bg-slate-600 text-white text-sm py-2 rounded-lg" onClick={onClose}>` → `<Button variant="secondary" className="w-full mt-5">OK</Button>`. `bg-slate-700` → `bg-surface`, `hover:bg-slate-600` → `hover:bg-surface-2`.
- [ ] Move inside `<ModalFooter>`.

---

## `frontend/src/pages/RoadmapTool/index.tsx`

**Lines that matter:** L42 (InfoIconButton import), L780–783 (loading state), L788–795 (root div + progress bar), L795 (h1), L812–820 (InfoIconButton usage), L821–850 (Calculate button), L860–865 (fallback message), L868–870 (empty-state div), L907–951 (binder tab strip), L812 (MethodologyInfoPopover call site).

### Deprecated imports (L42)

- [ ] Remove `import { InfoIconButton } from '../../components/InfoPopover'` (L42).
- [ ] Add `import { Popover } from '../../components/ui/Popover'`.
- [ ] Remove the `MethodologyInfoPopover` import (L36) and file usage once its content is inlined as a `<Popover>` (see below).

### Loading state (L779–783)

- [ ] `<div className="text-center text-slate-400 py-20">Loading wallet…</div>` → `text-slate-400` → `text-ink-muted`.

### Progress bar (L789–792)

- [ ] `<div className="h-full bg-indigo-500 animate-progress-bar" />` → `bg-indigo-500` → `bg-accent`.

### Page heading (L795)

- [ ] `<h1 className="text-2xl font-bold text-white shrink-0">Roadmap Tool</h1>` → `<Heading level={1}>Roadmap Tool</Heading>`. `text-white` → `text-ink`.

### InfoIconButton + MethodologyInfoPopover (L812–858)

- [ ] `<InfoIconButton onClick={(e) => { ... setMethodologyAnchor(...) }} label="How the roadmap is calculated" ... active={!!methodologyAnchor} />` → replace with `<Popover trigger={({ onClick, ref }) => <Button variant="icon" size="sm" onClick={onClick} ref={ref} aria-label="How the roadmap is calculated">...</Button>}>` containing the methodology text inline (move content from `MethodologyInfoPopover.tsx` here).
- [ ] Remove `methodologyAnchor` state (L215) and `{methodologyAnchor && <MethodologyInfoPopover .../>}` block (L853–858) once Popover is colocated.
- [ ] `MethodologyInfoPopover.tsx` can be deleted after its content is inlined.

### Calculate button (L821–850)

- [ ] This button has four distinct visual states. The Tailwind classes are:
  - Pending: `'bg-slate-700 text-slate-400 cursor-wait'` → `bg-surface text-ink-muted cursor-wait`.
  - Stale: `'bg-amber-500 hover:bg-amber-400 text-slate-900 shadow-sm shadow-amber-900/40'` → `bg-warn hover:bg-warn text-page shadow-sm`. `shadow-amber-900/40` → keep or map to a semantic shadow token if available.
  - Initial calc needed: `'bg-indigo-500 hover:bg-indigo-400 text-white shadow-sm shadow-indigo-900/40'` → `<Button variant="primary">` covers this state.
  - Up to date / disabled: `'bg-slate-700 text-slate-500 cursor-not-allowed'` → `bg-surface text-ink-faint cursor-not-allowed`.
  - **Recommendation**: Use `<Button>` but override `className` per state or add a `tone` prop. The amber "stale" state has no direct `Button` variant — it may need `variant="ghost"` with a `className` override of `bg-warn text-page`. Flag as open question.

### Fallback warning banner (L860–864)

- [ ] `<div className="mb-3 shrink-0 px-3 py-2 rounded-md border border-amber-700/60 bg-amber-900/30 text-amber-200 text-sm">` → `text-amber-200` → `text-warn`, `border-amber-700/60` → `border-warn/60`, `bg-amber-900/30` → `bg-warn/10` (if token opacity is supported, otherwise keep as-is and flag).

### Empty state panel (L868–870)

- [ ] `<div className="bg-slate-900 border border-slate-700 rounded-xl p-8 text-center text-slate-500 shrink-0">` → `bg-slate-900` → `bg-surface`, `border-slate-700` → `border-divider`, `text-slate-500` → `text-ink-faint`.
- [ ] `<Link to="/profile" className="text-indigo-400 hover:text-indigo-300">profile</Link>` → `text-indigo-400` → `text-accent`, `hover:text-indigo-300` → `hover:text-accent`.

### Binder tab strip (L907–951)

- [ ] Active tab class `'bg-slate-900 text-indigo-300 border-slate-700 -mr-px'` → `bg-surface text-accent border-divider`.
- [ ] Inactive tab class `'bg-slate-800/70 text-slate-400 border-slate-800 hover:text-slate-200 hover:bg-slate-800'` → `bg-surface-2/70 text-ink-muted border-divider hover:text-ink hover:bg-surface-2`.
- [ ] **Note**: These are binder-style vertical tabs with `rounded-l-md border border-r-0` — they are NOT a candidate for the `<Tabs>` primitive (which renders horizontal strips). Keep the bespoke markup and only apply token swaps.

---

## Open Questions / Missing Primitives

1. **Amber "stale" Calculate button state**: There is no `Button` variant for a warning/amber state. The stale Calculate button needs `bg-amber-500 text-slate-900` styling that isn't covered by `primary`, `secondary`, or `ghost`. Options: (a) add a `variant="warn"` to `Button`; (b) use `variant="ghost"` with `className="bg-warn text-page"` override; (c) keep as bespoke Tailwind inside a plain `<button>`.

2. **Rotating tag → `text-info` mismatch**: The Rotating badge uses `text-violet-300/90 bg-violet-500/10 border-violet-500/30`. The `text-info` token maps to blue/sky. If the design system expects violet for "rotating" status, either add a `tone="rotating"` to `Badge` or keep the badge as bespoke and flag for design review.

3. **Hardcoded hex chart colors**: `WalletTimelineChart` uses several hardcoded hex values for chart elements — `#fbbf24` (amber/yellow for SUB segment), `#818cf8` (indigo for active bar), `#4ade80` (green for cash), `#64748b` (slate for Today line). These are runtime-data-colored elements (group color comes from the data), not Tailwind class candidates. Keep as inline styles; consider extracting to a `chartPalette` constant if the design system adds CSS custom properties for chart colors.

4. **`bg-red-950` error state in WalletSummaryStats**: No direct `bg-neg-*` token for the darkest error background. Keep `bg-red-950` as-is unless the design system adds `bg-neg-subtle` or similar.

5. **IssuerRuleBanner API**: Need to verify `components/cards/IssuerRuleBanner.tsx` accepts a `RoadmapRuleStatus` object before deciding how to use it in `ApplicationRuleWarningModal.tsx` and `WalletTimelineChart.tsx`. Check whether severity is a computed prop or if it needs to be passed explicitly.

6. **Portal/amber violation container colors in ApplicationRuleWarningModal**: `bg-amber-900/20 border border-amber-700/40` — if these are semantic "warn-subtle" patterns, add a `bg-warn/10` token alias. Otherwise keep as-is and note them as hardcoded semantic colors.

7. **`Tabs` primitive for SignInDropdown tabs**: The sign-in / sign-up tabs in `SignInDropdown` currently use `border-b-2 border-indigo-500` as the active indicator. Verify whether `<Tabs>` renders the same border-bottom active style before adopting it; if not, keep as bespoke and apply token swaps only.

8. **Slider `accent-indigo-500`**: The range sliders in `WalletSummaryStats` (duration), `CurrencySettingsDropdown` (CPP), and `WalletPortalSharesEditor` (portal share) all use `accent-indigo-500`. If the design token `accent-accent` is defined in Tailwind config (e.g. `accent: { DEFAULT: 'var(--color-accent)' }`), swap to `accent-accent`. If not, the swap is blocked until the Tailwind config is extended — flag this.

9. **Binder tabs (index.tsx L907–951)**: These are vertical left-side tabs, not horizontal. The `<Tabs>` primitive only renders horizontal strips. Do not migrate these to `<Tabs>` — token-swap only.

10. **`MethodologyInfoPopover.tsx` deletion**: Once the content is inlined at `index.tsx` as a `<Popover>`, the file and its import should be deleted. This is a Task 40 cleanup item, not Task 37 (which covers the summary/ files).
